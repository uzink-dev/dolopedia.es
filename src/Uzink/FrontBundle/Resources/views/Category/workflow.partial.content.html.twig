{{ include('FrontBundle:Component:flash.partial.html.twig') }}
{{ form_start(form, {'attr': {'novalidate': 'novalidate'} }) }}
    {{ form_row(form.block_category) }}
    {{ form_row(form.block_seo) }}
    {{ form_rest(form) }}
    <div class="row container-row">
        <div class="col-md-12">
            <hr>
            <button type="submit" class="btn btn-primary separate-right">
                <span class="icon-save icon-big"></span> Guardar Cambios
            </button>
            <a href="{{ path('workflow.category.tree') }}" class="btn btn-grey"><span class="icon-cancel icon-big"></span> Cancelar Edición</a>
        </div>
    </div>
{{ form_end(form) }}